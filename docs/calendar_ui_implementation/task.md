# タスクリスト: Due DateのカレンダーUI導入

- [ ] `logic.js` に日付フォーマット関数 `formatDisplayDate` を追加
- [ ] `app.js` の初期データの日付形式を `YYYY-MM-DD` に変更
- [ ] `index.html` の入力フィールドを `type="date"` に変更
- [ ] `style.css` でカレンダー入力のデザインを調整
- [ ] 動作確認（カード表示が `MM.DD` 形式、入力がカレンダー形式であることを確認）
