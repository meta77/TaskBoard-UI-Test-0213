# Task List

- [ ] `logic.js` に純粋関数群を実装（`addFieldSetting`, `removeFieldSetting`, `updateFieldSetting`）
- [ ] `logic.js` にタスク用のデータ構造マイグレーション/サニタイズ関数を実装
- [ ] `logic.test.js` に追加した純粋関数群の単体テストを実装
- [ ] `app.js` の状態管理（Vueのref）に `descriptionSettings` と設定モーダル用のステートを追加
- [ ] `app.js` の `currentTask` のデータ構造を `descriptions: {}` を扱うように更新
- [ ] `index.html` のヘッダーに設定（Settings）ボタンを追加し、設定モーダルUIを構築
- [ ] `index.html` のタスク作成・編集モーダルで、設定された複数のDescription項目を動的に描画するように改修
- [ ] `npm test` 等で単体テストの実行と動作確認（ブラウザでの手動テスト）を行う
